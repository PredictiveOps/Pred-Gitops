# apiVersion: v1
# kind: Service
# metadata:
#   name: ml-service
#   namespace: default
#   labels:
#     app: ml-service
# spec:
#   type: ClusterIP
#   ports:
#     - port: 8000
#       targetPort: 8000
#       protocol: TCP
#       name: http
#   selector:
#     app: ml-service

apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: pred
  labels:
    app: ml-service
spec:
  type: ClusterIP
  selector:
    app: ml-service
  ports:
    - name: http
      port: 8003
      targetPort: 8003
      protocol: TCP
