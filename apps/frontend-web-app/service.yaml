apiVersion: v1
kind: Service
metadata:
  name: frontend-web-app
  namespace: default
  labels:
    app: frontend-web-app
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: frontend-web-app
