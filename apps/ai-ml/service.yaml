apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: pred
  labels:
    app: ml-service
spec:
  type: ClusterIP
  selector:
    app: ml-service
  ports:
    - name: http
      port: 8084
      targetPort: 8084
      protocol: TCP
