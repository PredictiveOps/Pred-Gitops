apiVersion: v1
kind: Service
metadata:
  name: authentication-service
  namespace: default
  labels:
    app: authentication-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: authentication-service
