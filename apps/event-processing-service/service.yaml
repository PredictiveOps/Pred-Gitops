apiVersion: v1
kind: Service
metadata:
  name: event-processing-service
  namespace: default
  labels:
    app: event-processing-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: event-processing-service
