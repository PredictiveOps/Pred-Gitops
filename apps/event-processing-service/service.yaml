# apiVersion: v1
# kind: Service
# metadata:
#   name: event-processing-service
#   namespace: pred
# spec:
#   selector:
#     app: event-processing-service
#   ports:
#     - port: 8080
#       targetPort: 8080
#       protocol: TCP
#   type: ClusterIP

apiVersion: v1
kind: Service
metadata:
  name: event-processing-service
  namespace: pred
  labels:
    app: event-processing-service
spec:
  type: ClusterIP
  selector:
    app: event-processing-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
