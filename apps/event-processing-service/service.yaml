apiVersion: v1
kind: Service
metadata:
  name: event-processing-service
  namespace: pred
spec:
  selector:
    app: event-processing-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
