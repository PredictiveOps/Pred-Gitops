apiVersion: v1
kind: Service
metadata:
  name: mosquitto-external
  namespace: pred
  labels:
    app: mosquitto
spec:
  type: LoadBalancer
  selector:
    app: mosquitto
  ports:
    - name: mqtts
      port: 8883
      targetPort: 8883
      protocol: TCP
